# Traffic-Eye Field Testing Architecture

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                       RASPBERRY PI 4                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Auto-Start Services                     │  │
│  │  ┌──────────────────────┐  ┌─────────────────────────┐   │  │
│  │  │ traffic-eye-field    │  │ traffic-eye-dashboard   │   │  │
│  │  │  (Main Detection)    │  │  (Web UI - Port 8080)   │   │  │
│  │  │   • YOLOv8n          │  │   • System metrics      │   │  │
│  │  │   • Helmet Classifier│  │   • Live logs           │   │  │
│  │  │   • Gemini OCR       │  │   • Service control     │   │  │
│  │  │   • GPS tracking     │  │                         │   │  │
│  │  └──────────────────────┘  └─────────────────────────┘   │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  Hardware Connected:                                            │
│  ┌────────────┐  ┌──────────┐  ┌────────────┐                 │
│  │ Pi Camera  │  │ USB GPS  │  │ Power      │                 │
│  │ Module v2  │  │ VK-162   │  │ 5V/3A      │                 │
│  │ (CSI Port) │  │ (USB)    │  │ (USB-C)    │                 │
│  └────────────┘  └──────────┘  └────────────┘                 │
│                                                                  │
│  Network:                                                       │
│  ┌──────────────────────────────────────────┐                  │
│  │         Tailscale VPN                    │                  │
│  │   IP: 100.x.x.x (Private mesh network)  │                  │
│  └──────────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ Tailscale VPN
                            │ (Encrypted tunnel)
                            │
┌─────────────────────────────────────────────────────────────────┐
│                           iPAD                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              Tailscale VPN Client                          │  │
│  │         (Connected to same mesh network)                   │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  Apps:                                                          │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │ Safari         │  │ Termius/Blink  │  │ Tailscale      │   │
│  │ (Dashboard)    │  │ (SSH Terminal) │  │ (VPN Control)  │   │
│  │                │  │                │  │                │   │
│  │ http://100.    │  │ ssh yashcs@    │  │ Status: ON     │   │
│  │   x.x.x:8080   │  │   100.x.x.x    │  │                │   │
│  └────────────────┘  └────────────────┘  └────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

```
┌───────────┐
│  Camera   │ ──► Frame Capture (15 FPS)
└───────────┘              │
                           ▼
                   ┌───────────────┐
                   │  YOLOv8n      │
                   │  Detection    │
                   └───────────────┘
                           │
              ┌────────────┴─────────────┐
              ▼                          ▼
     ┌────────────────┐        ┌─────────────────┐
     │ Motorcycle/    │        │ Traffic Light   │
     │ Person Found   │        │ Detection       │
     └────────────────┘        └─────────────────┘
              │
              ▼
     ┌────────────────┐
     │ Extract Head   │
     │ Region (Crop)  │
     └────────────────┘
              │
              ▼
     ┌────────────────┐
     │ Helmet         │ ──► ❌ NO HELMET?
     │ Classifier     │
     └────────────────┘
              │
              ▼
     ┌────────────────┐
┌───►│ Violation      │
│    │ Candidate      │
│    └────────────────┘
│             │
│             ▼
│    ┌────────────────┐
│    │ Save Best      │
│    │ Frame + GPS    │
│    └────────────────┘
│             │
│             ▼
│    ┌────────────────┐
│    │ Extract Plate  │
│    │ Region (Crop)  │
│    └────────────────┘
│             │
│             ▼
│    ┌────────────────┐
│    │ Gemini Cloud   │ ──► License Plate Text
│    │ OCR API        │
│    └────────────────┘
│             │
│             ▼
│    ┌────────────────┐
│    │ Store in       │
│    │ SQLite DB      │
│    └────────────────┘
│             │
│             ▼
│    ┌────────────────┐
│    │ Save Evidence  │
│    │ (JPEG + Video) │
│    └────────────────┘
│             │
└─────────────┘
```

## 🔄 Startup Flow

```
┌──────────────────┐
│  Power ON        │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Boot Sequence   │ (30-60 seconds)
│  • Load kernel   │
│  • Init systemd  │
│  • Network up    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Start Services  │ (Auto-start)
│  • gpsd          │
│  • tailscaled    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  traffic-eye     │
│  -field.service  │
│  • Load models   │
│  • Init camera   │
│  • Connect GPS   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  traffic-eye     │
│  -dashboard      │
│  • Start Flask   │
│  • Listen :8080  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  System Ready    │ ✅
│  • Detection ON  │
│  • Dashboard ON  │
│  • GPS tracking  │
└──────────────────┘
```

## 📂 File Structure

```
/home/yashcs/traffic-eye/
├── src/
│   ├── main.py                    # Main detection loop
│   ├── detection/
│   │   ├── detector.py            # YOLOv8n detector
│   │   └── helmet.py              # Helmet classifier
│   ├── ocr/
│   │   └── gemini_ocr.py          # Gemini Cloud OCR
│   └── web/
│       └── dashboard.py           # Web monitoring dashboard ⭐
│
├── scripts/
│   ├── deploy_field_testing.sh   # 🚀 Master deployment script
│   ├── check_power.sh             # Power health check
│   ├── setup_camera.sh            # Camera setup & test
│   ├── setup_gps.sh               # GPS setup & test
│   ├── setup_tailscale.sh         # VPN setup
│   └── cleanup_old_evidence.sh    # Auto-cleanup (cron)
│
├── systemd/
│   ├── traffic-eye-field.service      # Main service (auto-start) ⭐
│   └── traffic-eye-dashboard.service  # Dashboard service (auto-start) ⭐
│
├── config/
│   └── settings.yaml              # Configuration
│
├── data/
│   ├── evidence/                  # Violation evidence (JPEG + video)
│   ├── captures/                  # Debug captures
│   └── traffic_eye.db             # SQLite database
│
├── models/
│   ├── yolov8n_int8.tflite        # Object detection
│   └── helmet_cls_int8.tflite     # Helmet classifier
│
└── Documentation/
    ├── FIELD_DEPLOYMENT_COMPLETE.md   # 📖 Complete guide
    ├── FIELD_TESTING_CHECKLIST.md     # ✅ Pre-flight checklist
    ├── FIELD_QUICK_REFERENCE.md       # 📄 Quick reference (print)
    └── INSTALL_FIELD_TESTING.txt      # 🚀 Installation summary
```

## 🌐 Network Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Internet                                 │
│              (4G / WiFi / Ethernet)                         │
└────────────┬──────────────────────────────┬─────────────────┘
             │                              │
             ▼                              ▼
    ┌─────────────────┐          ┌─────────────────┐
    │  Tailscale      │          │  Tailscale      │
    │  Coordination   │◄────────►│  Coordination   │
    │  Server         │          │  Server         │
    └─────────────────┘          └─────────────────┘
             │                              │
             ▼                              ▼
    ┌─────────────────┐          ┌─────────────────┐
    │  Raspberry Pi   │          │      iPad       │
    │  100.x.x.1      │◄────────►│  100.x.x.2      │
    │  (Field unit)   │  Secure  │  (Monitor)      │
    │                 │  Tunnel  │                 │
    │  Services:      │          │  Apps:          │
    │  • Detection    │          │  • Safari       │
    │  • Dashboard    │          │  • SSH client   │
    │  • GPS          │          │  • Tailscale    │
    └─────────────────┘          └─────────────────┘
```

**Key Points**:
- ✅ **No port forwarding** required
- ✅ **End-to-end encryption** (WireGuard)
- ✅ **NAT traversal** (works behind firewalls)
- ✅ **Works over 4G/WiFi** (any network)
- ✅ **Private mesh network** (100.x.x.x subnet)

## 💾 Storage Management

```
┌──────────────────────────────────────────┐
│         64GB MicroSD Card                │
├──────────────────────────────────────────┤
│  OS + System (8GB)                       │
├──────────────────────────────────────────┤
│  Models (500MB)                          │
│  • YOLOv8n: 6.2MB                        │
│  • Helmet: 1.9MB                         │
├──────────────────────────────────────────┤
│  Evidence Storage (50GB+)                │
│  • Auto-cleanup after 7 days             │
│  • Delete oldest when < 5GB free         │
│  • Cron job: Daily at 3 AM               │
├──────────────────────────────────────────┤
│  Free Space (5GB+)                       │
│  • Buffer for operations                 │
│  • Log files                             │
└──────────────────────────────────────────┘

Evidence Structure:
  data/evidence/
  ├── 2026-02-09/
  │   ├── violation_uuid1/
  │   │   ├── best_frame.jpg
  │   │   ├── video.mp4
  │   │   └── metadata.json
  │   └── violation_uuid2/
  │       ├── best_frame.jpg
  │       ├── video.mp4
  │       └── metadata.json
  └── 2026-02-10/
      └── ...
```

## 🔋 Power Architecture

```
Option 1: Power Bank
┌────────────────┐
│  Power Bank    │  26800mAh @ 5V = 134Wh
│  (USB-C PD)    │  Pi consumption: ~15W (peak)
└────────┬───────┘  Runtime: ~8-10 hours
         │
         ▼
┌────────────────┐
│  USB-C Cable   │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│  Raspberry Pi  │
│  Power Input   │
│  5V/3A         │
└────────────────┘

Option 2: Car DC Supply
┌────────────────┐
│  12V Car       │  12V DC from cigarette lighter
│  Cigarette     │  (Engine must be running)
│  Lighter       │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│  DC-DC Buck    │  12V → 5V/4A
│  Converter     │  (With overcurrent protection)
└────────┬───────┘
         │
         ▼
┌────────────────┐
│  USB-C Cable   │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│  Raspberry Pi  │
│  Power Input   │
│  5V/3A         │
└────────────────┘

Power Monitoring:
  • vcgencmd get_throttled  → 0x0 = healthy
  • vcgencmd measure_temp    → < 80°C = good
  • Dashboard shows real-time power status
```

## 🎯 System Resources

```
Raspberry Pi 4 (4GB RAM):

┌─────────────────────────────────────────┐
│         CPU Usage (Typical)             │
│  ┌───────────────────────────────────┐  │
│  │ YOLOv8n Detection:     30-40%     │  │
│  │ Helmet Classifier:     10-15%     │  │
│  │ System + Dashboard:     5-10%     │  │
│  │ Total:                 45-65%     │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│        Memory Usage (Typical)           │
│  ┌───────────────────────────────────┐  │
│  │ OS + Services:        500 MB      │  │
│  │ Model Loading:        800 MB      │  │
│  │ Frame Buffers:        300 MB      │  │
│  │ Python Runtime:       200 MB      │  │
│  │ Total:              1800 MB       │  │
│  │ Available:          2200 MB       │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

Temperature Management:
  • Normal: 50-70°C
  • Warning: 70-80°C (reduce CPU quota)
  • Critical: > 80°C (throttling starts)
  • Solution: Add heatsink/fan, lower FPS
```

---

**Architecture Version**: 1.0
**Last Updated**: 2026-02-09
**Status**: Production Ready for Field Testing
